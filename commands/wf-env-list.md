---
description: List every configured Workfront environment (~/wf-envs/). Read-only.
---

Invoke:

```bash
bash ${CLAUDE_PLUGIN_ROOT}/skills/_shared/scripts/wf-env-list.sh
```

Show the output to the user verbatim. The script marks the active slug with `*`.
